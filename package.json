{
  "name": "dbapi",
  "version": "0.0.0",
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.3"
  }
}
